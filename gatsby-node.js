const path = require("path")

exports.createPages = async ({ graphql, actions }) => {
  const { createRedirect, createPage } = actions

  // createRedirect({
  //   fromPath: `/`,
  //   toPath: `/kurzy/vlastni-eshop/`,
  //   redirectInBrowser: true,
  //   statusCode: 302,
  //   force: true,
  // })
}
